name: calorie_counter_app
description: "AI-powered calorie counter for weight loss - วิเคราะห์อาหารด้วย AI"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  shared_preferences: ^2.3.2
  image_picker: ^1.1.2
  http: ^1.2.2
  flutter_dotenv: ^5.1.0
  intl: ^0.20.1
  percent_indicator: ^4.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
